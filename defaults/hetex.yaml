# Output settings
pdf-engine: xelatex

# 1. Use the Fixed Template

template: heb.latex

# 2. Run the Auto-Fix Script
filters:
  - /Users/rotemsemah/.pandoc/filters/he-auto.lua # <--- UPDATE YOUR USERNAME

# Variables
variables:
  documentclass: extarticle
  fontsize: 10pt
  papersize: a4
  geometry:
    - margin=2.5cm
    - top=2cm
    - bottom=2cm
